_base_: base.yaml

benchmark:
  name: omnibenchmark
  dataset_name: omnibenchmark
  data_root: /path/to/datasets/omnibenchmark
  scenario: class_incremental
  num_tasks: 21
  classes_per_task: null
  image_size: 224

training:
  batch_size: 64
  epochs_per_task: 10
  lr: 0.0008

runtime:
  num_workers: 8

warmup:
  num_batches: 5

output:
  benchmark_subdir: omnibenchmark
