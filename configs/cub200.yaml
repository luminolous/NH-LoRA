_base_: base.yaml

benchmark:
  name: cub200
  dataset_name: cub200
  data_root: /path/to/datasets/CUB_200_2011
  scenario: class_incremental
  num_tasks: 20
  classes_per_task: 10
  image_size: 224

training:
  batch_size: 64
  epochs_per_task: 20
  lr: 0.0008

runtime:
  num_workers: 8

warmup:
  num_batches: 5

output:
  benchmark_subdir: cub200
